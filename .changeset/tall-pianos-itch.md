---
"@intent-framework/dom": minor
---

Add opt-in `showSemanticIds` render option that emits `data-intent-screen`, `data-intent-ask`, and `data-intent-action` attributes matching `inspectScreen()` semantic IDs. Default DOM output is unchanged.
